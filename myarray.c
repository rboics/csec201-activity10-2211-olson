#include <stdio.h>
int main()
{
  char chars[] = {'A', 'B', 'C', 'D', '\0'};
  // 1. Write a for that terates over the chars array and prints out each letter individually
  // Must include two print statement, one with [] and one without.
  
  printf("\n");
 
  // 2. Write a for loop that use a pointer as a control variable to iterate and print each character in the list
  
  printf("\n");
  
  ptr = chars;
  // 3. Use a while loop to iterate over the chars array and print each char until we hit the end of the array
  
  printf("\n"); 
 
  return 0;
}
